#adminpanel
  = render 'admin_panel_controlls'
  = show_flash
  .tab-content
    %fieldset
      %legend
        = t('activerecord.attributes.gallery.edit') + @gallery.name
      = render 'form'
:javascript
  admit_gallery();
